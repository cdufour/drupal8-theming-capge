<div class="alert node-bg">NODE.HTML.TWIG</div>
<article {{ attributes }}>
    <h3>
        Titre: <a href="{{ url }}">{{ node.label }}</a>
    </h3>
    <p>Ecrit par {{ author_name }}</p>
    {{ content }}
</article>